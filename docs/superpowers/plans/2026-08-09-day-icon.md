# 날짜 칸 아이콘 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 편집 패널에서 아이콘을 클릭 한 번으로 날짜 칸에 찍고, 그 아이콘이 내보낸 PNG에도 나오게 한다.

**Architecture:** `DayEntry`에 선택 필드 `icon?: string`을 더하고, 뜻이 담긴 id로 번들 자산을 찾는 등록표(`theme/dayIcons.ts`)를 둔다. `DayCell`이 본문 영역에 글자 뒤로 아이콘을 깐다. 아이콘이 없으면 요소를 아예 만들지 않아 기존 결과물이 픽셀 단위로 같다.

**Tech Stack:** React 18, TypeScript 5.6, Vite 7, Vitest 3 (jsdom)

## Global Constraints

- 설계 문서: `docs/superpowers/specs/2026-08-09-day-icon-design.md`
- 테스트: `npm test`. 단일 파일은 `npx vitest run src/경로/파일.test.ts`
- 빌드(타입 검사 포함): `npm run build`
- **`icon`은 선택 필드**(`icon?: string`)다. `extra`와 같은 이유 — `migrateDoc`이 `days`를 통째로 캐스팅하므로 이 필드가 없는 문서가 실제로 존재한다.
- **`DOC_VERSION`을 올리지 않는다.**
- 아이콘 크기는 정확히 **166px**(`CELL_ICON_SIZE`). 파생식이 아니라 값을 직접 적는다.
- id는 정확히 이 아홉: `star`, `game`, `movie`, `together`, `note`, `pen`, `talk`, `rest-ko`, `rest-hanja`. 자산은 `src/assets/dayIcons/<id>.png`에 이미 들어 있다.
- **모르는 id는 아무것도 그리지 않는다.** 깨진 이미지가 결과물에 박히면 안 된다.
- 아이콘이 없을 때 렌더링 결과가 지금과 **문자열 단위로 같아야** 한다.
- 주석·커밋 메시지는 한국어. "왜"를 설명하고 "무엇"은 설명하지 않는다.
- 현재 247 테스트 통과.

## File Structure

| 파일 | 역할 | 변경 |
|---|---|---|
| `src/theme/dayIcons.ts` | id·라벨·자산 URL 등록표, `getDayIcon` | **신규** |
| `src/theme/dayIcons.test.ts` | 등록표 테스트 | **신규** |
| `src/model/types.ts` | `DayEntry.icon?: string` | 수정 |
| `src/model/defaults.ts` | `isBlankDayEntry`에 `icon` | 수정 |
| `src/model/recurring.ts` | `plan`·`planClear`가 `icon` 보존 | 수정 |
| `src/preview/layout.ts` | `CELL_ICON_SIZE` | 수정 |
| `src/preview/DayCell.tsx` | 본문 영역에 아이콘 렌더링 | 수정 |
| `src/editor/DayEditor.tsx` | 아이콘 고르는 줄 | 수정 |
| `docs/manual-checklist.md` | 점검 항목 | 수정 |

**Task 순서의 이유:** Task 1이 등록표를 만들고, Task 2가 데이터 필드와 **데이터가 사라질 수 있는 세 지점**을 한 번에 막는다. Task 3이 그리고, Task 4가 입력 경로를 잇는다. Task 5가 내보내기를 확인한다.

---

### Task 1: 아이콘 등록표

**Files:**
- Create: `src/theme/dayIcons.ts`, `src/theme/dayIcons.test.ts`

**Interfaces:**
- Consumes: `src/assets/dayIcons/*.png` (이미 커밋됨)
- Produces:
  - `export type DayIcon = { id: string; label: string; src: string }`
  - `export const DAY_ICONS: DayIcon[]` — 9개, 위 순서대로
  - `export function getDayIcon(id: string | undefined): DayIcon | undefined`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/theme/dayIcons.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { DAY_ICONS, getDayIcon } from './dayIcons'

describe('DAY_ICONS', () => {
  it('9종을 제공한다', () => {
    expect(DAY_ICONS).toHaveLength(9)
  })

  it('id가 정해진 아홉 개다', () => {
    // 저장된 문서가 이 문자열을 참조한다. 바꾸면 예전 문서의 아이콘이 사라진다.
    expect(DAY_ICONS.map((i) => i.id)).toEqual([
      'star', 'game', 'movie', 'together', 'note', 'pen', 'talk', 'rest-ko', 'rest-hanja',
    ])
  })

  it('id가 중복되지 않는다', () => {
    expect(new Set(DAY_ICONS.map((i) => i.id)).size).toBe(DAY_ICONS.length)
  })

  it('모두 라벨과 자산 경로를 갖는다', () => {
    for (const icon of DAY_ICONS) {
      expect(icon.label.length).toBeGreaterThan(0)
      expect(icon.src.length).toBeGreaterThan(0)
    }
  })
})

describe('getDayIcon', () => {
  it('id로 찾는다', () => {
    expect(getDayIcon('star')?.label).toBe('별')
  })

  it('모르는 id면 undefined다', () => {
    // 깨진 이미지를 결과물에 박느니 아무것도 안 그리는 쪽이 낫다.
    expect(getDayIcon('없는아이콘')).toBeUndefined()
  })

  it('undefined를 받아도 터지지 않는다', () => {
    expect(getDayIcon(undefined)).toBeUndefined()
  })
})
```

- [ ] **Step 2: 실패를 확인한다**

Run: `npx vitest run src/theme/dayIcons.test.ts`
Expected: FAIL — 모듈이 없어 import가 깨진다

- [ ] **Step 3: 등록표를 만든다**

`src/theme/dayIcons.ts`:

```ts
import game from '../assets/dayIcons/game.png'
import movie from '../assets/dayIcons/movie.png'
import note from '../assets/dayIcons/note.png'
import pen from '../assets/dayIcons/pen.png'
import restHanja from '../assets/dayIcons/rest-hanja.png'
import restKo from '../assets/dayIcons/rest-ko.png'
import star from '../assets/dayIcons/star.png'
import talk from '../assets/dayIcons/talk.png'
import together from '../assets/dayIcons/together.png'

export type DayIcon = {
  /** 저장된 문서가 참조하는 값. 한 번 정하면 바꾸지 않는다. */
  id: string
  /** 고르는 버튼의 aria-label */
  label: string
  /** Vite가 해시를 붙여 내보낸 자산 URL */
  src: string
}

/**
 * 날짜 칸에 찍을 수 있는 아이콘.
 *
 * id를 파일명(1.png…)이 아니라 뜻으로 둔 이유: 나중에 그림을 교체해도
 * 저장된 문서가 그대로 동작하고, 순서를 바꿔도 안전하다.
 */
export const DAY_ICONS: DayIcon[] = [
  { id: 'star', label: '별', src: star },
  { id: 'game', label: '게임', src: game },
  { id: 'movie', label: '영화', src: movie },
  { id: 'together', label: '합방', src: together },
  { id: 'note', label: '메모', src: note },
  { id: 'pen', label: '그림', src: pen },
  { id: 'talk', label: '저챗', src: talk },
  { id: 'rest-ko', label: '휴방', src: restKo },
  { id: 'rest-hanja', label: '휴', src: restHanja },
]

/**
 * id로 아이콘을 찾는다. 모르는 id면 undefined.
 *
 * 예전 문서나 손으로 고친 저장소에 등록되지 않은 id가 들어 있을 수 있다.
 * 그럴 때 깨진 이미지를 결과 PNG에 박느니 아무것도 안 그리는 쪽이 낫다.
 */
export function getDayIcon(id: string | undefined): DayIcon | undefined {
  if (id === undefined) return undefined
  return DAY_ICONS.find((icon) => icon.id === id)
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `npx vitest run src/theme/dayIcons.test.ts`
Expected: PASS

> jsdom에서 png import는 Vite가 문자열로 바꿔주므로 테스트에서도 값이 들어온다. `src`가 빈 문자열로 나오면 vite 설정 문제이니 보고할 것.

- [ ] **Step 5: 전체 테스트와 빌드**

Run: `npm test` → PASS
Run: `npm run build` → 성공. `dist/assets/`에 png 9개가 나오는지 확인해 보고할 것.

- [ ] **Step 6: 커밋**

```bash
git add src/theme/dayIcons.ts src/theme/dayIcons.test.ts
git commit -m "feat: 날짜 칸 아이콘 등록표 추가"
```

---

### Task 2: `icon` 필드와 데이터가 사라지는 세 지점

**이 계획에서 가장 위험한 Task다.** 필드를 더하면서 세 곳을 놓치면 사용자 데이터가 조용히 사라진다.

**Files:**
- Modify: `src/model/types.ts`, `src/model/defaults.ts`, `src/model/recurring.ts`
- Test: `src/editor/controls.test.ts`, `src/model/recurring.test.ts`, `src/model/storage.test.ts`

**Interfaces:**
- Consumes: 없음
- Produces: `DayEntry.icon?: string` — Task 3·4가 읽고 쓴다

**놓치면 안 되는 세 곳:**

| 위치 | 해야 할 일 | 안 하면 |
|---|---|---|
| `defaults.ts`의 `isBlankDayEntry` | `icon` 조건 추가 | 아이콘만 있는 칸을 `updateDay`가 빈 칸으로 보고 키를 지운다 |
| `recurring.ts`의 `plan` | `icon: doc.days[cell.date]?.icon` | 반복 규칙 재적용 시 아이콘이 사라진다 |
| `recurring.ts`의 `planClear` | `icon: entry.icon` | 「규칙 지우기」가 아이콘까지 지운다 |

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/editor/controls.test.ts`의 `describe('updateDay', ...)` 안:

```ts
  it('아이콘만 있어도 항목을 유지한다', () => {
    const doc = updateDay(createEmptyDoc(2026, 8), '2026-08-03', { icon: 'star' })
    expect(doc.days['2026-08-03']?.icon).toBe('star')
  })

  it('아이콘을 지우면 다른 게 없을 때 항목이 사라진다', () => {
    let doc = updateDay(createEmptyDoc(2026, 8), '2026-08-03', { icon: 'star' })
    doc = updateDay(doc, '2026-08-03', { icon: undefined })
    expect(doc.days['2026-08-03']).toBeUndefined()
  })
```

`src/model/recurring.test.ts`의 `describe('applyRecurringRules', ...)` 안:

```ts
  it('아이콘은 어느 모드에서도 보존된다', () => {
    for (const mode of ['fill-empty', 'overwrite'] as const) {
      const d = doc()
      d.days['2026-08-04'] = {
        text: '', dateColor: null, cellFill: null, marker: null, icon: 'game',
      }
      const out = applyRecurringRules(d, [rule()], mode)
      expect(out.days['2026-08-04'].icon).toBe('game')
      expect(out.days['2026-08-04'].text).toBe('발로란트 랭크\n21:00')
    }
  })
```

`src/model/recurring.test.ts`의 `describe('clearRecurringRules', ...)` 안:

```ts
  it('아이콘은 보존한다', () => {
    const d = applyRecurringRules(doc(), [rule()], 'fill-empty')
    d.days['2026-08-04'] = { ...d.days['2026-08-04'], icon: 'talk' }
    const out = clearRecurringRules(d, [rule()])
    expect(out.days['2026-08-04'].icon).toBe('talk')
    expect(out.days['2026-08-04'].text).toBe('')
  })
```

`src/model/storage.test.ts`의 `describe('migrateDoc', ...)` 안 — 이미 있는 `extra` 왕복 테스트 옆에:

```ts
  it('아이콘이 저장·복원을 견딘다', () => {
    // days를 통째로 캐스팅하기에 통과한다. 나중에 mergeHeader처럼 필드
    // 화이트리스트로 조이면 이 테스트가 먼저 깨져야 한다.
    const doc = createEmptyDoc(2026, 8)
    doc.days['2026-08-03'] = {
      text: '', dateColor: null, cellFill: null, marker: null, icon: 'movie',
    }
    saveDoc(doc)
    expect(loadDoc(2026, 8)?.days['2026-08-03'].icon).toBe('movie')
  })
```

- [ ] **Step 2: 실패를 확인한다**

Run: `npx vitest run src/editor/controls.test.ts src/model/recurring.test.ts src/model/storage.test.ts`
Expected: FAIL — `icon`이 `DayEntry`에 없어 타입 오류

- [ ] **Step 3: 타입에 필드를 더한다**

`src/model/types.ts`의 `DayEntry`, `extra` 다음:

```ts
  /**
   * 칸에 찍은 아이콘 id. theme/dayIcons.ts의 DAY_ICONS를 참조한다.
   *
   * extra와 같은 이유로 선택 필드다 — storage의 migrateDoc이 days를 통째로
   * 캐스팅해 넘기므로, 이 필드가 없는 예전 문서가 그대로 읽힌다.
   */
  icon?: string
```

- [ ] **Step 4: 빈 항목 판정에 넣는다**

`src/model/defaults.ts`의 `isBlankDayEntry`에 조건을 더한다:

```ts
  (entry.icon ?? '') === ''
```

기존 조건들과 `&&`로 잇는다. **이 함수는 `updateDay`와 `clearRecurringRules` 두 곳이 키 삭제 판단에 쓴다.**

- [ ] **Step 5: 반복 규칙이 보존하게 한다**

`src/model/recurring.ts`의 `plan`, `result.set(...)` 객체에 `extra` 옆:

```ts
      // 아이콘도 날짜에 묶인 값이라 규칙이 덮으면 안 된다.
      icon: doc.days[cell.date]?.icon,
```

같은 파일 `planClear`, `result.set(...)` 객체에 `extra` 옆:

```ts
      icon: entry.icon,
```

- [ ] **Step 6: 통과를 확인한다**

Run: `npx vitest run src/editor/controls.test.ts src/model/recurring.test.ts src/model/storage.test.ts`
Expected: PASS

- [ ] **Step 7: 전체 테스트와 빌드**

Run: `npm test` → PASS
Run: `npm run build` → 성공

- [ ] **Step 8: 커밋**

```bash
git add src/model/types.ts src/model/defaults.ts src/model/recurring.ts src/editor/controls.test.ts src/model/recurring.test.ts src/model/storage.test.ts
git commit -m "feat: 칸 항목에 아이콘 필드 추가"
```

---

### Task 3: 칸에 아이콘 그리기

**Files:**
- Modify: `src/preview/layout.ts`, `src/preview/DayCell.tsx`
- Test: `src/preview/layout.test.ts`

**Interfaces:**
- Consumes: Task 1의 `getDayIcon`, Task 2의 `DayEntry.icon`
- Produces: `layout.ts`의 `CELL_ICON_SIZE = 166`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/preview/layout.test.ts` — import에 `CELL_EXTRA_HEIGHT`, `CELL_ICON_SIZE`를 더하고 파일 끝 `describe` 안에 넣는다:

```ts
  it('아이콘이 추가 문구가 있는 칸에도 들어간다', () => {
    // 추가 문구가 있을 때 본문 높이와 같은 크기다. 이보다 크면 넘친다.
    expect(CELL_ICON_SIZE).toBe(CELL_TEXT_HEIGHT - CELL_EXTRA_HEIGHT)
  })

  it('아이콘이 칸 폭 안에 들어간다', () => {
    expect(CELL_ICON_SIZE).toBeLessThan(CELL_TEXT_WIDTH)
  })
```

- [ ] **Step 2: 실패를 확인한다**

Run: `npx vitest run src/preview/layout.test.ts`
Expected: FAIL — `CELL_ICON_SIZE`가 없어 import가 깨진다

- [ ] **Step 3: 상수를 더한다**

`src/preview/layout.ts`의 `CELL_EXTRA_MIN_SIZE` 다음:

```ts
/**
 * 칸에 찍는 아이콘 한 변.
 *
 * 추가 문구가 있을 때의 본문 높이(CELL_TEXT_HEIGHT − CELL_EXTRA_HEIGHT)와 같다.
 * 파생식으로 쓰지 않고 값을 직접 적는다 — 추가 문구 띠를 손볼 때 아이콘
 * 크기까지 딸려 움직이면 눈치채기 어렵다. 둘의 관계는 테스트가 지킨다.
 *
 * 추가 문구가 없으면 본문이 244px이라 위아래 여백만 늘어난다. 크기를 키우지
 * 않는 이유는 한 달치를 훑을 때 칸마다 아이콘 크기가 달라 보이면 안 되기 때문이다.
 */
export const CELL_ICON_SIZE = 166
```

- [ ] **Step 4: 통과를 확인한다**

Run: `npx vitest run src/preview/layout.test.ts`
Expected: PASS

- [ ] **Step 5: 칸에 그린다**

`src/preview/DayCell.tsx`:
- import에 `CELL_ICON_SIZE`(from `./layout`)와 `getDayIcon`(from `../theme/dayIcons`)을 더한다
- `const { bodyHeight, extraHeight } = splitCellText(extra)` 다음에:

```tsx
    const icon = cell.inMonth ? getDayIcon(entry?.icon) : undefined
```

- 본문 `AutoFitText`를 아래 구조로 감싼다. **아이콘이 없으면 감싸지 않고 지금 그대로 둔다** — 기존 결과물이 픽셀 단위로 같아야 한다:

```tsx
      {icon === undefined ? (
        <AutoFitText
          text={text}
          maxWidth={CELL_TEXT_WIDTH}
          maxHeight={bodyHeight}
          baseSize={CELL_TEXT_BASE_SIZE}
          minSize={CELL_TEXT_MIN_SIZE}
          lineHeight={CELL_TEXT_LINE_HEIGHT}
          color={cell.inMonth ? theme.bodyText : theme.outsideMonthText}
          markerColor={cell.inMonth ? (entry?.marker ?? null) : null}
        />
      ) : (
        <div style={{ position: 'relative', width: CELL_TEXT_WIDTH, height: bodyHeight }}>
          {/*
            아이콘은 글자 뒤에 깐다. 아이콘을 쓸 때는 글자를 안 쓰는 게 보통이지만,
            둘 다 있어도 글자가 읽혀야 한다.
          */}
          <img
            src={icon.src}
            alt=""
            style={{
              position: 'absolute',
              left: '50%',
              top: '50%',
              transform: 'translate(-50%, -50%)',
              width: CELL_ICON_SIZE,
              height: CELL_ICON_SIZE,
              pointerEvents: 'none',
            }}
          />
          <div style={{ position: 'relative' }}>
            <AutoFitText
              text={text}
              maxWidth={CELL_TEXT_WIDTH}
              maxHeight={bodyHeight}
              baseSize={CELL_TEXT_BASE_SIZE}
              minSize={CELL_TEXT_MIN_SIZE}
              lineHeight={CELL_TEXT_LINE_HEIGHT}
              color={cell.inMonth ? theme.bodyText : theme.outsideMonthText}
              markerColor={cell.inMonth ? (entry?.marker ?? null) : null}
            />
          </div>
        </div>
      )}
```

칸의 바깥 `<div>`에 이미 `overflow: 'hidden'`이 있으므로 아이콘이 칸을 넘지 않는다.

- [ ] **Step 6: 전체 테스트와 빌드**

Run: `npm test` → PASS
Run: `npm run build` → 성공

- [ ] **Step 7: 커밋**

```bash
git add src/preview/layout.ts src/preview/DayCell.tsx src/preview/layout.test.ts
git commit -m "feat: 날짜 칸에 아이콘을 그린다"
```

---

### Task 4: 아이콘 고르는 UI

**Files:**
- Modify: `src/editor/DayEditor.tsx`

**Interfaces:**
- Consumes: Task 1의 `DAY_ICONS`, Task 2의 `DayEntry.icon`, 기존 `updateDay`
- Produces: 없음 (UI 종단)

순수 UI라 단위 테스트를 새로 쓰지 않는다. 판정은 Task 2가, 조회는 Task 1이, 렌더링은 Task 3이 이미 덮었다.

- [ ] **Step 1: 아이콘 줄을 넣는다**

`src/editor/DayEditor.tsx`의 `추가 문구` `input` **다음**, 첫 `SwatchRow` **앞**에 넣는다. `DAY_ICONS`를 `../theme/dayIcons`에서 import한다:

```tsx
              <div style={{ display: 'flex', alignItems: 'center', gap: 6, marginTop: 8 }}>
                <span style={{ fontSize: 12, color: '#52525b', width: 56, flexShrink: 0 }}>
                  아이콘
                </span>
                <div style={{ display: 'flex', gap: 4, flexWrap: 'wrap' }}>
                  <button
                    type="button"
                    aria-label="아이콘 없음"
                    aria-pressed={(entry?.icon ?? '') === ''}
                    onClick={() => patch(cell.date, { icon: undefined })}
                    style={{
                      width: 28, height: 28, borderRadius: 4, cursor: 'pointer', padding: 0,
                      border: (entry?.icon ?? '') === ''
                        ? '2px solid #18181b'
                        : '1px solid #d4d4d8',
                      background: '#ffffff', fontSize: 12, lineHeight: 1,
                    }}
                  >
                    ×
                  </button>
                  {DAY_ICONS.map((icon) => (
                    <button
                      key={icon.id}
                      type="button"
                      aria-label={icon.label}
                      aria-pressed={entry?.icon === icon.id}
                      title={icon.label}
                      onClick={() => patch(cell.date, { icon: icon.id })}
                      style={{
                        width: 28, height: 28, borderRadius: 4, cursor: 'pointer', padding: 1,
                        border: entry?.icon === icon.id
                          ? '2px solid #18181b'
                          : '1px solid #d4d4d8',
                        background: '#ffffff',
                      }}
                    >
                      <img
                        src={icon.src}
                        alt=""
                        style={{ width: '100%', height: '100%', display: 'block' }}
                      />
                    </button>
                  ))}
                </div>
              </div>
```

맨 앞 `×`가 「없음」이다. 기존 `SwatchRow`가 같은 관습을 쓴다.

- [ ] **Step 2: 테스트와 빌드**

Run: `npm test` — 개수가 그대로여야 한다
Run: `npm run build` → 성공

- [ ] **Step 3: 커밋**

```bash
git add src/editor/DayEditor.tsx
git commit -m "feat: 날짜별 아이콘 고르는 줄 추가"
```

---

### Task 5: 수동 점검 문서

**Files:**
- Modify: `docs/manual-checklist.md`

**Interfaces:**
- Consumes: Task 1–4
- Produces: 없음

내보내기 실제 확인은 컨트롤러가 브라우저에서 한다. 이 Task는 문서만 담당한다.

- [ ] **Step 1: 절을 새로 넣는다**

`## 추가 문구` 절 **다음**, `## 강조` **앞**에 넣는다. 기존 형식(제목 수준, 체크박스, 굵게 처리, 실패가 뜻하는 바 설명)을 그대로 따른다:

```markdown
## 날짜 칸 아이콘

- [ ] 아이콘을 클릭하면 그 날짜 칸 가운데에 원본 색 그대로 나온다
- [ ] `×`를 누르면 아이콘이 사라진다
- [ ] **아이콘이 없는 칸은 이 기능을 넣기 전과 모양이 같다**
- [ ] 추가 문구가 있는 칸에서도 아이콘이 잘리지 않는다
- [ ] 추가 문구가 없으면 아이콘 위아래 여백만 늘고 크기는 그대로다
- [ ] 아이콘과 일정 글자가 둘 다 있으면 글자가 위에 온다
- [ ] **아이콘만 있는 칸이 새로고침 후에도 남아 있다** — 사라지면 빈 항목 판정에서 아이콘을 빠뜨린 것이다
- [ ] 반복 규칙을 다시 적용해도 아이콘이 남는다
- [ ] 「규칙 지우기」가 아이콘을 지우지 않는다
- [ ] 「지난달 내용 가져오기」가 아이콘을 요일에 맞춰 옮긴다
- [ ] **PNG로 내보낸 이미지에 아이콘이 들어 있다** — 빠지면 html-to-image가 이미지를 인라인하지 못한 것이다
```

- [ ] **Step 2: 커밋**

```bash
git add docs/manual-checklist.md
git commit -m "docs: 날짜 칸 아이콘 점검 항목 추가"
```

---

## 완료 조건

- [ ] `npm test` 전부 통과
- [ ] `npm run build` 타입 오류 없음
- [ ] 아이콘이 없는 칸의 렌더링이 이 기능 이전과 동일
- [ ] 아이콘만 있는 칸이 저장·복원을 견딤
- [ ] **내보낸 PNG에 아이콘이 들어 있음** (컨트롤러가 픽셀로 확인)
